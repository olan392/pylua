# Pylua
```python
from pylua import LuaRuntime
r = LuaRuntime()
r.execute("""
-- lua code here!
""")
```
# how to install
1. go to the pylua folder
2. make sure you have `clang` or `gcc`
3. use `pip install module/*.whl`
